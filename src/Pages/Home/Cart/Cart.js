import React from "react";
import "./Cart.css";
const Cart = (Data) => {
  // const [key, photo, service, fee] = props;
  return (
    <div className="container">
      <div>
        <h1>{Data.langth}</h1>
      </div>
    </div>
  );
};

export default Cart;
